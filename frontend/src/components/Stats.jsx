import { Fragment, h } from "preact";
import { useState } from "preact/hooks";

export default function Stats() {
  const [weight, setWeight] = useState("");
  return (
    <Fragment>
      <div class="w-screen h-[40vh] bg-white block"></div>
    </Fragment>
  );
}
